<%= form_for Geolocation.new, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>

<% if @geolocation.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@geolocation.errors.count, "error") %> prohibited
        this geolocation from being saved:
      </h2>
      <ul>
        <% @geolocation.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.text_field :message %>
  <%= f.text_field :lat %>
  <%= f.text_field :lng %>

  <%= f.submit "Create" %>
<% end %>
